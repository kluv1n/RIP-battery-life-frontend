import { writeFileSync } from "node:fs";

const id = String(Date.now());
writeFileSync(".env.pages.local", `# auto-generated by build:pages\nVITE_BUILD_ID=${id}\n`, "utf8");
console.log("pages build id:", id);
