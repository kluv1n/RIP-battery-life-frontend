# Скопируй в .env.zerotier.local (в .gitignore)
# Токен: https://my.zerotier.com/account → Create API Access Token
ZEROTIER_API_TOKEN=
# Опционально — если сеть уже создана; иначе скрипт создаст сам
# ZEROTIER_NETWORK_ID=
