[package]
name = "battery_life_guest"
version = "0.1.0"
description = "Battery Life guest (lab 8)"
authors = ["RIP"]
edition = "2021"

[lib]
name = "battery_life_guest_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "battery-life-guest"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
